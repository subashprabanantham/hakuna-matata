### Running the service through Maven

```
# To execute the profile 'peer1' & 'peer2' - pass on as profiles variable

mvn spring-boot:run -Dspring-boot.run.profiles=peer1

mvn spring-boot:run -Dspring-boot.run.profiles=peer2
```